# Run: 2026-07-21 (post-0.7.0)

**Result: PASS** — zero hard fails, fully on-system output. 11 guesses reported by the agent;
1 assessed as a true docs gap at review (down from 5 assessed on 2026-07-16).

Same task, same fresh-agent restriction, run after 0.7.0 shipped (Panel, Toolbar,
`filter-toolbar` live) per the cadence note in the surface-cycle spec. Zero invented props —
every prop cross-checked clean against `dist/manifest.json`, including the non-obvious
`Tag variant="accent" subtle` combination and `Field group/description`. Zero hardcoded
colors, zero off-vocabulary size or variant names. The agent additionally used the new
`Panel` as the form surface and followed the `settings-form-row` pattern from
`dist/patterns.json` unprompted.

**4 of the 5 residual 07-16 topics closed** — the agent *cited* the documented rules where
the previous run had guessed: (1) programmatic `data-psi-theme` mechanics including the
root-effect idiom and the paint-`--psi-bg-primary`-yourself rule; (2) field gap numbers
(`psi-gap-24` fields / `psi-gap-12` toggle groups / `psi-gap-8` button and label+Tag rows);
(4) CSS import order (order-independent via the explicit `@layer` statement); (5)
content-measure width (app-level by design; `.psi-container` is the only tokenized width).

**The one surviving gap, (3) Tag-as-badge, narrowed to a spelling problem**: the badge
mapping said highlight → `accent-subtle`, but Tag's variant union has no `-subtle` entries —
subtle is a boolean prop on Tag, so the agent had to infer that `accent-subtle` is spelled
`variant="accent" subtle`. Filed and fixed in the same cycle as this run: `guidance.tags`
gained a `tagApi` note and the llms.txt badge-mapping line now states the Tag spelling.

The other 10 reported guesses were assessed as app-level judgment, not docs gaps: surface
choice (Panel vs bare form), app-root/container placement, theme-effect placement inside a
single-component deliverable, Cancel→`neutral` (covered by guidance: neutral = secondary
actions), uniform size 40 (covered: "40 for comfortable forms"), label+Tag row `inline-flex`
mechanics (gap-8 + pair-with-flex are documented; the inline/center detail is cosmetic), one
`Field group` per toggle (follows `settings-form-row`), Tag children (covered by the native
pass-through rule), and form state/copy (never a docs concern). Minor observation, not
counted: Checkbox/Switch list `children` in the manifest while Tag does not, though all three
take children — a strict cross-checker could stumble there.

Trend: assessed docs-gap count 6 → 5 → 1. Exit condition for the 07-16 topics is met for
4 of 5; the fifth's fix lands with this run, so the next run (due after the next recipe/doc
change) should report zero guesses on all five.
