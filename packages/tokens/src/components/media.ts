/** Media treatment tokens (D35). --psi-media-tint is a brand-defined filter
 * chain; the .psi-media-tint utility applies it with hover reveal. */
export const mediaVars: Record<string, string> = {
  tint: "none",
};
