/** Card component tokens (--psi-card-*). Layout shell, not a link (WS5). */
export const cardVars: Record<string, string> = {
  border: "var(--psi-border-faint)",
  bg: "transparent",
  gap: "var(--psi-space-16)",
  radius: "var(--psi-radius-8)",
};
